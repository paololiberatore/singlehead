analyze(
    'disconnected connection of strictly entailed bodies (disconnected.fig)',
    None,   # False?
    'abc=def', 'def=ghi', 'ghi=jk', 'jk=mn', 'mn=abc',
    'a->d', 'e->g', 'i->c', 'j->m', 'q->n')

